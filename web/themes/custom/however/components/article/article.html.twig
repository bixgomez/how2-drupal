<article class="article">

	<h2 class="dev-heading">This is an article (not a section)</h2>

	{% if not hide_title %}
		{% if title_display %}
			{{ title_display }}
		{% elseif title %}
			{{ title }}
		{% endif %}
	{% endif %}

	{% include "@issue-meta/issue-meta.html.twig" with {
		journal_name: journal,
		journal_url: journal_url,
		volume_issue: volume_issue,
		issue_pub_date: issue_pub_date,
		issue_url: issue_url,
		archive_url: archive_url[0]['#url'].toString(),
		person_bio_link: "#bios"
	} %}

	<div class="content">

		{% if images['#items'] and not images['#items'].isEmpty %}
			<div class="content-section">
				<h3 class="dev-heading">page_images</h3>
				<p class="dev-text">
					Do we want images for articles?  Will articles ever have images?
				</p>
				{{ images }}
			</div>
		{% endif %}

		{# {% if person_bio.0['#paragraph'].field_person.entity.title.value %}
			<div class="content-section">
					<p class="author-name">{{ person_bio.0['#paragraph'].field_person.entity.title.value }}</p>
			</div>
		{% endif %} #}

		{% if working_notes['#items'] and not working_notes['#items'].isEmpty %}
			<div class="content-section">
				{{ working_notes }}
			</div>
		{% endif %}

		{% if person_bio['#items'] and not person_bio['#items'].isEmpty %}
			<div id="bios" class="content-section">
				{{ person_bio }}
			</div>
		{% endif %}

		{% if node_body_sections|length > 1 %}
			<div class="content-section">
				<ul class="body-section-links">
					{% for section in node_body_sections %}
						<li>
							<a href="#section-{{ loop.index }}">{{ section.entity.field_title.value }}</a>
						</li>
					{% endfor %}
				</ul>
			</div>
		{% endif %}

		{% for key, item in node_body_sections %}
			{% if key is same as(0) or key is same as(1) %}
					{% set paragraph = item.entity %}
				<div id="section-{{ loop.index }}" class="content-section">
					{{ drupal_entity('paragraph', paragraph.id.value) }}
				</div>
			{% endif %}
		{% endfor %}

	</div>

</article>
